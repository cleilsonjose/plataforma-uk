# Plataforma UK 🇬🇧

Sistema completo de relacionamentos, focado em performance e segurança.

## 🚀 Estrutura do Projeto

O projeto é dividido em duas partes principais:

- **/backend**: API REST desenvolvida com Java 21 e Spring Boot 3.
- **/frontend**: Aplicação Web desenvolvida com Angular 19 e Material Design.

## 🛠️ Tecnologias Utilizadas

### Backend
- Java 21 (LTS)
- Spring Boot 3
- Spring Security + JWT
- PostgreSQL / H2 (Database)
- Lombok

### Frontend
- Angular 19
- Angular Material (UI)
- TypeScript
- SCSS

## 🏁 Como Rodar o Projeto

### Pré-requisitos
- Node.js 24+
- Java 21
- Maven

### Rodando o Backend
1. Entre na pasta `backend`.
2. Execute o comando: `./mvnw spring-boot:run` ou via STS4.
3. A API estará disponível em `http://localhost:8080`.

### Rodando o Frontend
1. Entre na pasta `frontend`.
2. Instale as dependências: `npm install`.
3. Rode o servidor: `ng serve`.
4. Acesse em `http://localhost:4200`.

---
Desenvolvido por Cleilson Jose.